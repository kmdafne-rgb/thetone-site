# THETONE Landing — 수정 내역

## 2026-09-22 (v19)

### III. EXECUTION 마무리 문장 수정
- KO: `나머지는 THETONE이 챙깁니다.` → **`나머지는 THETONE이 책임집니다.`**
- EN: `We'll handle the rest.` → **`We take care of the rest.`**

---

## 2026-09-22 (v18)

### 중앙 컬럼 blur 제거 — 얼룩 현상 해결
- `backdrop-filter: blur(3px)` → **`none`**
- 원인: 중앙 컬럼은 스크롤되는데 배경 사진은 화면 고정이라, 블러가 매 프레임 다른 지점을 샘플링하며 얼룩과 띠가 발생. 아치 그늘처럼 명암 차가 큰 구간에서 특히 두드러졌음.
- 결과: 중앙은 검정 45% 반투명 판만 깔려 균일하게 표시. 가독성은 동일.

---

## 2026-09-22 (v17)

### 상단바 밴드 제거 (v12 롤백)
- v12에서 추가한 중앙 컬럼 폭 밴드를 삭제. 상단바는 **완전 투명**.
- 이유: 로고는 왼쪽 윙 중앙(`wingW`), 메뉴는 오른쪽 끝에 있는데 밴드는 중앙 컬럼 폭(`colWidth`)이라 **아무것도 받치지 않는 사각형**이었음. 1708px 기준 밴드 x 461–1247 / 로고 x 0–112 / 메뉴 x 1345–1695로 셋 다 겹치지 않음.
- 가독성은 기존 상단 그라디언트(227px, `rgba(14,13,10,0.35)`→투명)가 담당.

### 문구 파일 손상 복구
- 이전 편집에서 `</html>` 뒤에 옛 문구 블록이 중복 삽입되고, 한국어 `stage3` 자리에 영문이 들어가 있었음 → 화면에 III. EXECUTION 수정이 반영되지 않던 원인.
- 중복 블록 제거, 한국어 문구 정정. 두 파일 모두 `</html>`에서 정확히 종료.

---

## 2026-09-22 (v16)

### ESSENCE 3단계 문구 개정 (EN/KO)
- **I. POSITION** — 마무리를 `This is how we define where your brand belongs.` / `이렇게 브랜드가 서야 할 자리를 먼저 정합니다.`로 교체. 기존의 포지셔닝·프레스 설명 절은 삭제(EPICENTER와 중복).
- **II. ACCESS** — `we bring buyers to our Milan showroom`으로 단순화하고, 리오더 문장을 `not just this season's order` 대비 구조로 재작성. KO도 동일한 대비로 정합.
- **III. EXECUTION** — 계약·보고 순서를 정리(`All terms are fixed by agreement before the season, with orders, shipments, and payments reported in writing throughout.`). 마무리 문장을 `Design the collection. We'll handle the rest.` / `디자이너는 컬렉션에만 집중하세요. 나머지는 THETONE이 챙깁니다.`로 교체.

### 인트로 문구 축약
- EN `...lasting commercial value.` / KO `...지속 가능한 상업적 가치로 바꿉니다.` — 뒷절 삭제.

---

## 2026-09-22 (v14)

### 골격선이 상단바를 관통
- 골격선 레이어 `z-index` **6 → 11** (상단바 z-index 10 위로)
- 이전에는 세로 레일이 상단바에서 끊겨, 빛이 흐를 때 바 아래에서 멈췄음.
- 이제 레일이 화면 최상단까지 이어지고 빛도 끊김 없이 통과. 바의 밴드는 그대로 아래에 있어 메뉴 가독성은 유지되고 1px 선만 그 위를 지나감.

---

## 2026-09-22 (v13)

### 섹션 정렬 — center로 통일
- `#top`(히어로), `#essence`, `#epicenter`: `flex-end` → **`center`**
- `#connect`: `flex-end` 유지 (연락처·푸터가 아래로 쌓이는 구조라 하단 정렬이 자연스러움)
- 검토 과정: `flex-start`(상단)도 시도했으나 텍스트가 상단바에 붙고 화면 아래 2/3가 빈 검정으로 남아 폐기. `center`가 위아래 여백이 균등하고 텍스트 블록이 배경 아치 높이와 맞음.

---

## 2026-09-22 (v12)

### 상단바 — 중앙 컬럼 폭으로 제한
- 바 자체는 `background: transparent`. 배경·블러·하단 테두리를 **중앙 컬럼 폭(`colWidth`)에만** 걸리는 별도 레이어로 분리.
- 이유: 전면 너비 띠가 사진 위를 가로질러 윙의 기둥과 골격선을 끊고 있었음. 스켈톤이 윙에만 존재하는 규칙과도 어긋났음.
- 결과: 좌우 윙은 사진이 그대로 뚫려 보이고, 바가 컬럼 체계 안으로 들어옴.
- 밴드에 `z-index:-1`을 줘서 로고·메뉴 글자가 위에 오도록 정리.

---

## 2026-09-22 (v11)

### CONNECT 섹션 문구 정리
- 부제 줄 삭제 (EN `begin in MILANO.` / KO `밀라노에서.`) → **EN·KO 모두 `CONNECT.`만 표시**
- 빈 문단이 여백으로 남지 않도록 `connectShow`로 해당 `<p>`를 `display:none` 처리. 문구가 다시 들어오면 자동으로 보임.

---

## 2026-09-22 (v10)

### 배경 이미지 교체 — 가로 원본
- `bg-v3.jpg`(1432×1322, 거의 정사각) → **`bg-wide.jpg`(1280×720, 16:9)**
- 새 Gemini 렌더(1456×720)에서 좌우를 잘라 정확히 16:9로 맞춤. 세로는 손대지 않음.
- 배경 처리 순서: 원본 16:9 → 화면 비율에 맞춰 `cover` → `transform: scale(1.22)`로 추가 확대 → `background-position: 50% 50%` 중앙 기준 크롭.
- **해결된 문제**: 정사각 소스 시절 넓은 화면에서 강제되던 높이 45% 손실이 사라짐. 기둥이 주두(柱頭)부터 주초(柱礎)까지 온전히 프레임에 들어와 위아래 균형이 맞음.
- `scale(1.22)`는 기둥을 가깝게 당겨 좌우 가장자리 군더더기를 제거한 값.

### 중앙 컬럼 스크림
- `scrimAlpha` **0.62 → 0.45**
- 중앙과 윙의 명도 차를 줄이려는 의도였으나 **체감 차이는 작음**. 중앙이 어두운 주원인은 스크림이 아니라 그 자리에 오는 사진의 아치 그늘로 확인됨.
- 후속 과제: 확대율을 1.0으로 되돌리거나 `background-position`의 가로값을 조정해 밝은 소실점 구간을 중앙에 배치.

---

## 2026-09-22 (v9)

### 섹션 정렬 통일 — 하단 고정
`#top`(히어로), `#essence`, `#epicenter`의 `justify-content`를 `center` → **`flex-end`**.
- 기존에 `flex-end`였던 `#connect`와 동일한 방식으로 통일.
- 결과: 네 섹션 모두 콘텐츠가 뷰포트 하단에 붙어, 스크롤할 때 각 섹션의 시작 지점이 일정하게 읽힘.
- 인트로 섹션(`min-height` 없는 짧은 섹션)은 `center` 유지.

---

## 2026-09-21 (v8)

### 1. 배경 크롭 — 소실점 기준 정렬
- `background-position: 50% 42%` → **`50% 79%`**
- 기준: 회랑의 소실점(안개가 밝게 모이는 지점)을 화면 정중앙에 두는 값.
- 원본 구성: 천장 위쪽 약 45%, 바닥 아래쪽 약 20%, 소실점은 위에서 63% 지점(살짝 내려다보는 앵글).
- 정사각 소스를 가로 화면에 `cover`로 덮으면 높이 약 45%가 프레임 밖으로 넘치고, 그 중 79%를 위쪽에서 잘라내면 소실점이 화면 중앙에 옴.
- 결과: 상단 아치 곡선 유지 / 하단 바닥·주초 유지 / 차분한 톤.
- 대안값 `72%` — 소실점이 중앙보다 위, "올려다보는 웅장함"이 강해짐.

### 2. 선 정리 — 스켈톤은 윙에만
- **제거**: 컬럼 안 섹션 구분선 4개 (`data-glowline` 0~3). 구분선은 스크롤되고 윙 골격선은 화면 고정이라, 스크롤 중 두 체계가 어긋난 위치에 걸려 불규칙하게 보였음.
- **가로 골격선을 윙 안쪽에서 끊음**: 전면 횡단 → 윙 폭만큼. 중앙 텍스트 컬럼을 관통하지 않음.
- **좌우 비대칭** (왼쪽 2 / 오른쪽 3): 왼쪽 `16vh`, `72vh` / 오른쪽 `26vh`, `54vh`, `95vh`. 세로 레일 2개는 윙·컬럼 경계, 좌우 대칭 유지.
- 위치값은 기존 플랫폼 스크린샷을 측정해 맞춤(v8). 왼쪽 첫 선이 오른쪽 첫 선보다 높아 좌우 리듬이 드러남.
- 레퍼런스는 세로 레일이 비대칭(왼쪽 윙 29% / 오른쪽 21.6%)이지만, 컬럼 위치 변경 영향이 커서 대칭 유지로 결정.
- 결과: 화면에 남는 선은 윙 골격 7개(세로 2 + 가로 5).

### 3. 골격선 자동 점등 — "중앙에서 바깥으로"
커서를 움직여야만 빛이 들어오던 문제 해결. 규칙은 한 문장: **빛은 항상 중앙 컬럼에서 바깥으로.**

- 세로 레일: 화면 중앙 높이에서 출발해 위·아래로 동시에 뻗어나감
- 가로선: 컬럼에 붙은 안쪽 끝에서 출발해 화면 바깥 끝으로
- 주기 **5.2초**, 모든 선이 동시에 출발하고 도착하며 페이드아웃 (심장 박동)
- `prefers-reduced-motion` 설정 시 자동 정지
- 커서 반응 효과는 그대로 위에 얹힘

**작업 중 해결한 함정 2건**
- `background-attachment: fixed`가 걸린 선에서는 그라디언트 기준 영역이 선이 아니라 화면 전체가 되어, `%` 위치로 지정한 빛이 선 밖에 그려짐 → 레이어별 기준을 분리(`fixed, scroll`)해 커서(화면 기준)와 흐름(선 기준)을 동시에 유지.
- 초기안은 선 하나씩 순차 점등이라 점멸로 읽힘 → 전 선 동시 흐름으로 변경.

### 변경 없음
- 상단바 (배경 `rgba(14,13,10,0.1)`, `blur(6px)`, `border-bottom`) — 세션 중 투명 처리했다가 지시에 따라 원복
- 중앙 컬럼 스크림 (`scrimAlpha` 0.62, `blur(3px)`)
- 타이포 스케일, 간격 규칙, 컬럼 너비(46vw / 모바일 72vw·56vw)
- EN/KO 문구, 내비게이션 구조

### 미해결 / 다음 작업
- **윙 5색 레이어**: 현재 `deploy/index.html`의 윙에는 단색 베일 하나(`veilAlpha` 0.06)만 있음. 이전 버전에 있던 5색 레이어를 찾아 되살릴 예정.
- **배경 소스**: `bg-v3.jpg`가 1432×1322(거의 정사각)이라 넓은 화면에서 가로 1.8배 확대 + 높이 45% 손실이 강제됨. UI 없는 가로 원본(2560px 이상)을 넣으면 크롭 자체가 불필요해짐.

---

## 이전 누적 (요약)
- 색 없는 타이포 체계 → 구조선만 마젠타
- 임의값 8단계 스케일 → 비례 5단계 체계
- 라틴 + Noto Sans KR → Pretendard
- 패딩·마진 규칙 통일
- 배경 영상 의존 → 정지 이미지로 안정화
- EN/KO 문장 단위 정합
